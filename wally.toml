[package]
name = "synpixel/faker"
description = "A library that generates Roblox data such as group icons and display names, inspired by faker-js."
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
authors = ["synpixel"]

exclude = ["**"]
include = ["src", "src/*", "out", "out/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]